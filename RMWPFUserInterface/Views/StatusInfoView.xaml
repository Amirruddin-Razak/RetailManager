<UserControl x:Class="RMWPFUserInterface.Views.StatusInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RMWPFUserInterface.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="Header" Grid.Row="1" Grid.Column="1"
                   FontSize="24" FontWeight="Bold" Margin="10"
                   HorizontalAlignment="Center"/>
        <TextBlock x:Name="Message" Grid.Row="2" Grid.Column="1"
                   FontSize="20" HorizontalAlignment="Center" Margin="20"/>
        <Button x:Name="Close" Grid.Row="3" Grid.Column="1"
                   FontSize="24" Padding="10" Margin="30">
            Close
        </Button>
    </Grid>
</UserControl>
